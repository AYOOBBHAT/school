# Web app — copy to .env.local for local dev. On Vercel, set these in Project → Environment Variables.
# Use https for the Railway backend (required when the site is served over HTTPS).

VITE_API_URL=https://school-production-55d2.up.railway.app
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
